[package]
name = "hnsw_rs"
version = "0.1.0"
edition = "2021"

[lib]
name = "hnsw_rs"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.23", features = ["extension-module"] }
numpy = "0.23"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
